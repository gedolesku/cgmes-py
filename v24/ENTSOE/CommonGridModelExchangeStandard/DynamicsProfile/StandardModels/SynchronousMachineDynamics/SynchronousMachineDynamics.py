#######################################################
# 
# SynchronousMachineDynamics.py
# Python implementation of the Class SynchronousMachineDynamics
# Generated by Enterprise Architect
# Created on:      20-May-2025 17:40:51
# Original author: ppbr003
# 
#######################################################
from typing import List, Optional, Any
from dataclasses import dataclass
from enum import Enum, auto
from ENTSOE.CommonGridModelExchangeStandard.DynamicsProfile.StandardModels.RotatingMachineDynamics import RotatingMachineDynamics
from ENTSOE.CommonGridModelExchangeStandard.DynamicsProfile.Wires.SynchronousMachine import SynchronousMachine

@dataclass
class SynchronousMachineDynamics(RotatingMachineDynamics):
    """Synchronous machine whose behaviour is described by reference to a standard
    model expressed in one of the following forms:
      <ul>
      	<li>simplified (or classical), where a group of generators or motors is not
    modelled in detail</li>
      </ul>
      <ul>
      	<li>detailed, in equivalent circuit form</li>
      	<li>detailed, in time constant reactance form</li>
      </ul>
      <font color="#0f0f0f">or by definition of a user-defined model.</font>
      <font color="#0f0f0f">
      </font><font color="#0f0f0f"><b>Note:</b>  It is a common practice to
    represent small generators by a negative load rather than by a dynamic
    generator model when performing dynamics simulations. In this case a
    SynchronousMachine in the static model is not represented by anything in the
    dynamics model, instead it is treated as ordinary load.</font>
      <font color="#0f0f0f">
      </font><font color="#0f0f0f"><b>Parameter Notes:</b></font>
      <ol>
      	<li><font color="#0f0f0f">Synchronous machine parameters such as <b>Xl,
    Xd</b>,<b> Xp</b> etc. are actually used as inductances (L) in the models,
    </font> but are commonly referred to as reactances since, at nominal frequency,
    the per unit values are the same. However, some references use the symbol L
    instead of X. </li>
      </ol>
    """
    # Synchronous machine to which synchronous machine dynamics model applies.
    SynchronousMachine: Optional[SynchronousMachine] = None
     