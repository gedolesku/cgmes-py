[project]
name = "python-cgmes"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "lxml>=4.9.0",
    "rdflib>=6.0.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
