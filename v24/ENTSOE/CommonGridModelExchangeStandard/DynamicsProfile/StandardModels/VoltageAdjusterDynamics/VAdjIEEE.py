#######################################################
# 
# VAdjIEEE.py
# Python implementation of the Class VAdjIEEE
# Generated by Enterprise Architect
# Created on:      21-May-2025 11:00:24
# Original author: pcha006
# 
#######################################################
from typing import List, Optional, Any
from dataclasses import dataclass
from enum import Enum, auto
from ENTSOE.CommonGridModelExchangeStandard.DomainProfile.Simple_Float import Simple_Float
from ENTSOE.CommonGridModelExchangeStandard.DomainProfile.PU import PU
from ENTSOE.CommonGridModelExchangeStandard.DomainProfile.Seconds import Seconds
from ENTSOE.CommonGridModelExchangeStandard.DynamicsProfile.StandardModels.VoltageAdjusterDynamics.VoltageAdjusterDynamics import VoltageAdjusterDynamics

@dataclass
class VAdjIEEE(VoltageAdjusterDynamics):
    """The class represents IEEE Voltage Adjuster which is used to represent the
    voltage adjuster in either a power factor or var control system.
    
      Reference: IEEE Standard 421.5-2005 Section 11.1.
    """
    # Set high to provide a continuous raise or lower
    # (<i>V</i><i><sub>ADJF</sub></i>).
    vadjf: Simple_Float  = None
 
    # Rate at which output of adjuster changes (<i>ADJ_SLEW</i>).  Unit = sec./PU.
    # Typical Value = 300.
    adjslew: Simple_Float  = None
 
    # Maximum output of the adjuster (<i>V</i><i><sub>ADJMAX</sub></i>).  Typical
    # Value = 1.1.
    vadjmax: PU  = None
 
    # Minimum output of the adjuster (<i>V</i><i><sub>ADJMIN</sub></i>).  Typical
    # Value = 0.9.
    vadjmin: PU  = None
 
    # Time that adjuster pulses are on (<i>T</i><i><sub>AON</sub></i>).  Typical
    # Value = 0.1.
    taon: Seconds  = None
 
    # Time that adjuster pulses are off (<i>T</i><i><sub>AOFF</sub></i>).  Typical
    # Value = 0.5.
    taoff: Seconds  = None
     